# Copy this file to .env and fill in your own value.
# config.py reads API_KEY from the environment and falls back to a safe demo value,
# so the project still runs on a fresh clone without any setup.
API_KEY=put-your-real-api-key-here
